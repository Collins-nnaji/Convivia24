# Use .env.production for both local testing and production.
# npm run dev loads .env.production automatically (one source of truth).
DATABASE_URL=postgresql://user:password@host/db?sslmode=require
OPENAI_API_KEY=sk-proj-...
