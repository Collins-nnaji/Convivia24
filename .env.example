# Copy this to .env.local for local development (never commit .env.local).
# For deployment: set these in your host's dashboard (Vercel, Netlify, etc.).

DATABASE_URL=postgresql://user:password@host/db?sslmode=require
