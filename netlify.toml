[build]
  image = "ubuntu-22"
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  CI = "false"
  NODE_VERSION = "20.9.0"

# Next.js handles all routing automatically - no redirects needed 