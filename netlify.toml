[build]
  image = "ubuntu-22"
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Remote hosts the Netlify Image CDN may fetch from (bottle shots on Azure Blob and the CDN).
[images]
  remote_images = ["https://.*"]

[build.environment]
  CI = "false"
  NODE_VERSION = "22"

# Redirect Netlify default host to your custom domain. Add your domain in Netlify → Domain management first.
[[redirects]]
  from = "/*"
  to = "https://convivia24.com/:splat"
  status = 301
  force = true
  conditions = {Host = ["convivia24app.netlify.app"]}
