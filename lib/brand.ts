/** Primary wordmark in `/public` — used on desktop and wide layouts. */
export const BRAND_LOGO_SRC = '/convivia24.png';

/** Compact mark (e.g. 24+) — reads clearly at small sizes on mobile. */
export const BRAND_LOGO_MARK_SRC = '/Logo2.png';
