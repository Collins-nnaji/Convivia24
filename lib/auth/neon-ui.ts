/**
 * Social sign-in buttons for @neondatabase/auth/react/ui.
 * Provider ids must match Better Auth / Neon (e.g. Google enabled in Neon dashboard).
 */
export const NEON_AUTH_SOCIAL_GOOGLE = {
  providers: ['google'],
} as const;
